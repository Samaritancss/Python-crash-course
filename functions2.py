# def hello(name):
#     print(f"Hello {name}")

# person = "nat"

# hello(person)


# def hello(phrase, name):
#     print(f"{phrase} {name}")

# greeting = "hello"
# person = "nat"

# hello(greeting, person)

#with return
# def hello(phrase, name):
#     response = f"{phrase} {name}"
#     return response

# greeting = "Hello"
# person = "Nat"

# response = hello(greeting, person)

# print(response)


# class Person:
#     def __init__(self, name, age, size):
#         self.name = name
#         self.age = age
#         self.size = size

# student = Person("nat", 22, "large")

# print(vars(student))


#dealing with operating system
# import os

# command = "ping"

# def ping(host):
#     response = os.popen(f"{command} {host}.").read()
#     return response

# while True:
#     host = input("Host to ping: ")
#     response = ping(host)
#     print(response)


#dealing with host
import os

host = ["cnn.com", "fox.com", "tacobell.com"]

command = "ping"

while True:
    page = '''
            



           '''
